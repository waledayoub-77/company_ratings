================================================================================
  RATEHUB — V3 MANUAL TEST CASES
  Branch  : ratehub.6
  Date    : March 7, 2026
  Credentials:
    Company Admin  : tech.admin@ratehub.com  / Test1234!
    System Admin   : admin@ratehub.com       / Test1234!
    Employee       : frank@ratehub.com       / Test1234!
================================================================================


════════════════════════════════════════════════════════════════════════════════
  AUTOMATED TESTS — ALL PASSING (no manual verification needed)
════════════════════════════════════════════════════════════════════════════════

  API tests (test-all-v3.js): 34/34 ✅
  Playwright e2e (17-v3-features.spec.js): 21/21 ✅

  Verified by automation:
  ✅  F1 API  — POST /employments/invite returns 201 (registered user)
  ✅  F1 API  — POST /employments/invite returns 400 (unregistered / duplicate)
  ✅  F1 API  — GET pending-invites, resend, cancel all working
  ✅  F2 API  — End employment endpoint responds correctly
  ✅  F2 API  — GET list-all employments returns array
  ✅  F3 API  — GET /companies/filter-options returns countries + cities + industries
  ✅  F3 UI   — Companies page Filters panel opens with Country/City dropdowns
  ✅  F5 API  — POST /jobs creates position (201)
  ✅  F5 API  — GET /jobs?companyId=X returns data array
  ✅  F5 API  — GET /jobs/all auto-detects companyId for admin
  ✅  F5 UI   — Jobs tab visible and navigable on company admin dashboard
  ✅  F5 UI   — Job Board tab visible on employee dashboard
  ✅  F7 API  — POST /eoty/events returns 201 or 400
  ✅  F7 API  — GET eoty events + winners
  ✅  F7 UI   — EOTY tab visible and navigable on both dashboards
  ✅  F11 API — GET /feedback/received, /given, /coworkers all 200
  ✅  F11 UI  — Feedback page shows anonymous toggle
  ✅  F12 UI  — No "Request Verification" button on employee dashboard
  ✅  F12 UI  — Invite-only banner visible on Employment tab
  ✅  F12 UI  — Company admin Employees tab shows Invite Employee section
  ✅  F14 API — GET /notifications returns 200 with data array
  ✅  F14 UI  — Notification bell visible after login
  ✅  CP  UI  — Company profile page loads with title


════════════════════════════════════════════════════════════════════════════════
  REMAINING MANUAL TESTS — bugs found during manual testing
════════════════════════════════════════════════════════════════════════════════


────────────────────────────────────────────────────────────────────────────────
F2 — Company Admin: End Employment (UI issues)
────────────────────────────────────────────────────────────────────────────────
API works. UI needs refinement.

[ ] F2-A   Login as tech.admin → Employees tab → click End Employment
           → should show inline confirmation with optional Reason field
           BUG: No reason field — just a bare End button
           FIX NEEDED: Add optional reason text input before confirmation

[ ] F2-B   End employment confirmation should be an inline UI element
           (soft confirmation banner), NOT a browser alert() popup
           BUG: Currently uses window.alert/confirm popup
           FIX NEEDED: Replace alert with in-page confirmation component


────────────────────────────────────────────────────────────────────────────────
F3 — Country & City Filter Dropdowns (UI broken)
────────────────────────────────────────────────────────────────────────────────
API returns correct data (verified). Frontend dropdowns are empty.

[-- working but not all countries and cities are available ] F3-A   Go to /companies → open Filters panel
           → Country and City dropdowns should be populated with data
           BUG: Both dropdowns are EMPTY despite API returning data
           FIX NEEDED: Wire filterOptions.countries/cities into dropdown options

[-- ] F3-B   Select a country → City dropdown narrows to that country's cities
           BUG: Cannot test — dropdowns are empty
           BLOCKED BY: F3-A

[-- ] F3-C   Select a city → company list filters to that city only
           BUG: Cannot test — dropdowns are empty
           BLOCKED BY: F3-A

[-- ] F3-D   Apply country filter + search bar simultaneously
           → results narrow by both filters
           BUG: Cannot test — dropdowns are empty
           BLOCKED BY: F3-A


────────────────────────────────────────────────────────────────────────────────
F4 — Revoked Email Lock (Send Invite button broken)
────────────────────────────────────────────────────────────────────────────────

[-- I can invite but when sending it, it will appear in Employment Requests and admin can approve or reject it (when admin send invite to employee it should appear in the company admin side as invite pending and discard options when employee approves it then it will appear to the company admin as approved (take into considiration that employee can only accept the invitation it will appear to the employeee as accept button only he can not reject it) )  ] F4-A   Login as tech.admin → Invite Employee → enter a revoked email
           → should show error: "This email has been revoked"
           BUG: Send Invite button has no click handler / does nothing
           FIX NEEDED: Wire the Send Invite button onClick to API call

[ ] F4-B   A revoked email CAN be invited to a DIFFERENT company
           BLOCKED BY: F4-A (button not working)


────────────────────────────────────────────────────────────────────────────────
F5 — Job Postings: Create Button (UI broken)
────────────────────────────────────────────────────────────────────────────────
API works (verified). Create button in UI does nothing.

[-- ] F5-A   Login as tech.admin → Jobs tab → fill form → click Post Job / Create
           → new job should appear in Open Positions list
           BUG: Create/Post button has no functionality — nothing happens on click
           FIX NEEDED: Wire the create button onClick to createJobPosition API

[-- ] F5-B   After F5-A: job visible on /companies/<id> Open Positions section
           BLOCKED BY: F5-A

[no jobs are shown in Job board section ] F5-C   Employee Dashboard → Job Board → find position → click Apply
           → CV upload + cover letter form appears
           BLOCKED BY: F5-A

[no jobs are shown in Job board section ] F5-D   Submit application → appears in My Applications with status: pending
           BLOCKED BY: F5-A

[no jobs are shown in Job board section ] F5-E   Admin → Jobs tab → view application → Move to Interview
           BLOCKED BY: F5-A

[no jobs are shown in Job board section ] F5-F   Approve application → employee auto-added to company
           BLOCKED BY: F5-A

[no jobs are shown in Job board section ] F5-G   Reject application → applicant notified
           BLOCKED BY: F5-A

[-- ] F5-H   Close Position → job removed from public listing
           BLOCKED BY: F5-A


────────────────────────────────────────────────────────────────────────────────
F6 — EOTM: Remove Date Picker (UI issue)
────────────────────────────────────────────────────────────────────────────────
Backend enforces current month. Frontend still shows date picker.

[-- working but put the error when the admin put old year or future year tell heim current year only not write for him month to let him understand(if chosed month old or i future write for him month if year write year) ] F6-A   Login as tech.admin → EOTM tab → Create EOTM Event
           → should auto-use current month with NO date picker
           BUG: Date picker is still visible and selectable
           FIX NEEDED: Remove month/year picker; auto-fill current month/year

[ ] F6-B   After removing picker: create event → shows as March 2026 automatically


────────────────────────────────────────────────────────────────────────────────
F7 — EOTY: Create Event Button (UI broken) 
────────────────────────────────────────────────────────────────────────────────
API works (verified). Create button in UI does nothing.

[-- ] F7-A   Login as tech.admin → EOTY tab → click Create EOTY Event
           → should create event for current year
           BUG: "Start 2026 Year" button does nothing when pressed
           FIX NEEDED: Button text should say "Create EOTY Event" (no year
           in label); onClick should call createEotyEvent API

[-- ] F7-B   After F7-A: nominee list appears with all verified employees
           BLOCKED BY: F7-A

[i pressed it and no soft error appears and tell the admin that event already exist ] F7-C   Try to create duplicate event for 2026 → error "already exists"
           BLOCKED BY: F7-A

[-- ] F7-D   Employee EOTY tab: nominee list visible, NO vote counts shown
           BLOCKED BY: F7-A

[no i press vote and nothing happens ] F7-E   Employee clicks Vote → recorded; button changes to "Voted"
           BLOCKED BY: F7-A

[ no i tried to vote agian nosoft error appears] F7-F   Employee tries second vote → error "already voted"
           BLOCKED BY: F7-A

[in admin dashboard after closing the event the real names are not shown only employee with his votes appear i need real name with his real votes not 'employee' instead of name ] F7-G   Admin closes event → winner revealed with vote count
           BLOCKED BY: F7-A

[-- ] F7-H   Company profile → Hall of Fame shows winner
           BLOCKED BY: F7-A


────────────────────────────────────────────────────────────────────────────────
F8 — Hide Vote Counts While Open 
────────────────────────────────────────────────────────────────────────────────

[ remove 0 votes that is fixed (yes whe I vote no number appears for the count but there is 0 votes fixed remove it)] F8-A   While EOTM/EOTY event is open: employee sees names only, no counts
           BLOCKED BY: F7-A (need a fresh open event)

[no votes appeares maybe we can let it appear in the winner hall after the admin closes the voting and declare the winner under the winner name in the winnier hall appears number of votes ] F8-B   After admin closes event: vote counts become visible
           BLOCKED BY: F7-A


────────────────────────────────────────────────────────────────────────────────
F9 — Winner Certificate (not implemented)
────────────────────────────────────────────────────────────────────────────────

[ no certificate is appearing - for the winner when he enters EOTM or EOTY and he is the winner he must see something that let him take a certificate or like a screenshot to the winner section ] F9-A   Login as EOTM/EOTY winner → Dashboard shows "You Won!" widget
           BUG: No certificate component or "You Won" section exists
           FIX NEEDED: Build certificate component on employee dashboard

[ ] F9-B   Click View Certificate → shows winner name, company, award type, period
           BUG: Not implemented

[ ] F9-C   Click Download → PNG certificate downloads
           BUG: Not implemented


────────────────────────────────────────────────────────────────────────────────
F10 — Automatic Employee Count on Company Profile 
────────────────────────────────────────────────────────────────────────────────

[nothing appears ] F10-A  Go to /companies/<companyId> → employee count shown
           BUG: No employee count displayed on company profile page
           FIX NEEDED: Query count of approved current employees and show it


────────────────────────────────────────────────────────────────────────────────
F11 — Anonymous Peer Feedback (needs manual verification)
────────────────────────────────────────────────────────────────────────────────
API + toggle verified by automation. Full flow needs manual check.

[ ] F11-A  Login as frank@ratehub.com → Feedback tab → enable anonymous toggle
           → select colleague → write and submit feedback
           → success toast; is_anonymous = true in DB

[ ] F11-B  Login as the feedback recipient
           → received feedback shows "Anonymous Colleague" (not sender name)

[ ] F11-C  Submit feedback with anonymous toggle OFF
           → recipient sees sender's actual name

[ ] F11-D  Login as admin@ratehub.com → admin panel
           → admin CAN see who sent anonymous feedback (anonymity is recipient-only)


────────────────────────────────────────────────────────────────────────────────
F14 — Notification Deep Links (wrong navigation)
────────────────────────────────────────────────────────────────────────────────
Bell + dropdown + API all work. Deep link navigation goes to wrong pages.

[ ] F14-A  Click a "feedback received" notification
           → should navigate to /dashboard with Feedback tab showing RECEIVED feedback
           BUG: Navigates to /dashboard/feedback (submit page) instead of
           the received feedback view
           FIX NEEDED: Deep link should open received-feedback view, not submit form

[ ] F14-B  Click a "application status changed" notification
           → should navigate to /dashboard with Job Board tab active
           BUG: Navigates to wrong page
           FIX NEEDED: Deep link route should point to the correct tab

[ ] F14-C  Click an "invited to join" notification
           → should navigate to /accept-invite?token=...
           BLOCKED BY: F4-A (invite button not working to generate test data)


════════════════════════════════════════════════════════════════════════════════
  FEATURES THAT NEED NO FURTHER TESTING (working / env-dependent)
════════════════════════════════════════════════════════════════════════════════

  ✅  F1   — Invite Employee: API + UI section verified (send, cancel, resend)
  ✅  F12  — Invite-only model: no self-request button, banner shown, API blocked
  ✅  F13  — AI Sentiment: works when OPENAI_API_KEY is set (env-dependent)
  ✅  F14  — Notifications: bell, dropdown, mark-all-read, API all working
  ✅  F10-A — No manual employee count override in Settings (removed)
