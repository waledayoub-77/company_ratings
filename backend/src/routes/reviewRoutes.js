// Review Routes - AYA's Module
// Defines API endpoints for reviews
// Day 0: Empty file - ready for Day 2 implementation

const express = require('express');
const router = express.Router();
const reviewController = require('../controllers/reviewController');

// Middleware will be imported on Day 2
// const { requireAuth } = require('../middlewares/authMiddleware');

// TODO Day 2: Define these routes
// POST   /               → createReview (auth required)
// PATCH  /:id            → updateReview (auth required)
// GET    /my-reviews     → getMyReviews (auth required)

module.exports = router;
