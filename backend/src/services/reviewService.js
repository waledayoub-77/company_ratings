// Review Service - AYA's Module
// Business logic for review operations
// Day 0: Empty file - ready for Day 2 implementation

const { supabase } = require('../config/database');

// TODO Day 2: Implement these functions
// - createReview(data) - Create new review
// - updateReview(id, data, userId) - Update review (48hr window)
// - getMyReviews(employeeId) - Get employee's reviews
// - checkVerifiedEmployment(employeeId, companyId) - Check if can review
// - checkDuplicateReview(employeeId, companyId) - Prevent duplicate reviews
// - recalculateCompanyRating(companyId) - Update company average rating

module.exports = {
  // Export functions here on Day 2
};
