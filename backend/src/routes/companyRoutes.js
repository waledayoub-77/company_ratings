// Company Routes - AYA's Module
// Defines API endpoints for companies
// Day 0: Empty file - ready for Day 1 implementation

const express = require('express');
const router = express.Router();
const companyController = require('../controllers/companyController');

// Middleware will be imported on Day 1
// const { requireAuth } = require('../middlewares/authMiddleware');

// TODO Day 1: Define these routes
// GET    /               → getAllCompanies
// GET    /:id            → getCompany
// POST   /               → createCompany (admin only)
// PATCH  /:id            → updateCompany (admin only)

module.exports = router;